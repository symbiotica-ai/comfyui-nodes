// ABOUTME: Remotion entry point that registers the root component
// ABOUTME: Required by Remotion CLI for rendering

import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
